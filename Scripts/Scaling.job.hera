#!/bin/csh
# Usage: msub job.hera

#MSUB -l nodes=4
#MSUB -l partition=hera
#MSUB -l walltime=0:30:00
#MSUB -q pdebug
#MSUB -V
#MSUB -o /g/g99/toth2/Tmp/CRASH/SCALING/run_n64/runlog

date
cd /g/g99/toth2/Tmp/CRASH/SCALING/run_n64

# Number of cores = 16 times the number of nodes
srun -n64 CRASH.exe

exit
