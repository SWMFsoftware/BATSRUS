module ModSize

  use BATL_size

  ! Named indexes for coordinate directions
  use BATL_geometry, ONLY: x_, y_, z_, r_, Phi_, Theta_, Lon_, Lat_

end module ModSize
!==============================================================================
